/*funciones en el struct
struct Linkedlist {
    Node * head;
    Linkedlist():head(nullptr){};
}
void push(int x){
    if(headd==nullptr){
        head = new Node(x);
        return;
    }
}
Node * currentNode = head;
while (currentNode=>sig != nullptr){
    currentNode = currentNode=>sig;
}
currentNode=>sig = new Node(x);